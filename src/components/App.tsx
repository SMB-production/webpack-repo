import {Counter} from './ui-components/Counter'
import '../index.scss'

export const App = () => {
   return (
      <div className="app">
         <Counter />
         <Counter />
         <Counter />
      </div>
   )
}
